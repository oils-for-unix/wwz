# for cgit, from Claude
Options +ExecCGI
AddHandler cgi-script .cgi

# Problems with making URLs pretty:
# - Using cgit/ can cause redirect loops
# - Using git/ doesn't change the URLs that cgit gives out?

# RewriteEngine On
# RewriteRule ^git/(.*)$ /cgit/cgit.cgi/$1 [L]

AddDefaultCharset UTF-8
